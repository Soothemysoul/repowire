replicaCount: 1

image:
  repository: us-docker.pkg.dev/baldmaninc/gcr.io/repowire-web
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

hostname: repowire.io
